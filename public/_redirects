# Cloudflare Pages _redirects
# Case-insensitive routes for static pages
/MCP      /mcp.html   200
/Mcp      /mcp.html   200

# Static pages served first (Cloudflare checks static files before redirects)
# Everything else falls through to the SPA terminal
/*  /index.html  200
