{
  "name": "shiro",
  "version": "0.1.0",
  "description": "Browser-native Unix shell with Git, pipes, and IndexedDB filesystem - no servers, runs entirely in JavaScript",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node increment-build.js && tsc && vite build",
    "preview": "vite preview",
    "test": "echo 'Tests have moved to the windwalker repo.' && echo '' && echo 'To run Shiro tests:' && echo '  cd ../windwalker' && echo '  npm install' && echo '  npm run test:shiro' && echo '' && echo 'Windwalker uses linkedom for proper DOM polyfills (no window errors).' && exit 1",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "esbuild-wasm": "^0.27.2",
    "http-cache-semantics": "^4.1.1",
    "isomorphic-git": "^1.27.1"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-node-polyfills": "^0.25.0",
    "vitest": "^4.0.18"
  }
}
